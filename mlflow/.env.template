# ============================================================================
# MLflow GCP Deployment - Environment Variables Template
# ============================================================================
# Copy this file to .env and fill in your secure passwords
#
# Usage:
#   cp .env.template .env
#   # Edit .env with your passwords
#   bash setup_mlflow_gcp.sh
#
# Security Notes:
# - NEVER commit .env to version control
# - Use strong passwords (at least 16 characters)
# - Include mix of letters, numbers, and special characters
# - Rotate passwords regularly (every 90 days recommended)
# ============================================================================

# Root password for Cloud SQL PostgreSQL instance
# This is the postgres superuser password
ROOT_PASSWORD=your_strong_root_password_here

# User password for MLflow database user (mlflow-bek)
# This is used by the MLflow server to connect to the database
USER_PASSWORD=your_strong_user_password_here

# ============================================================================
# Password Generation Tips
# ============================================================================
# Generate strong random passwords using:
#
#   # On macOS/Linux:
#   openssl rand -base64 32
#
#   # Or using Python:
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
#
#   # Or use a password manager like 1Password, LastPass, etc.
# ============================================================================
